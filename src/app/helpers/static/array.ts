import groupBy from 'lodash.groupby';
import sortBy from 'lodash.sortby';

/**
 * @category Collection
 */
export const sortArray = sortBy;

/**
 * @category Collection
 */
export const groupArray = groupBy;
